<% if @model.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      <%= "#{@model.errors.count} #{Russian.pluralize(@model.errors.count, 'ошибка', 'ошибки', 'ошибок')}!" %> 
    </div>
    <ul>
    <% @model.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>