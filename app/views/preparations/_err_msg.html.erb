<% if @prep.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      <%= "#{@prep.errors.count} #{Russian.pluralize(@prep.errors.count, 'ошибка', 'ошибки', 'ошибок')}!" %> 
    </div>
    <ul>
    <% @prep.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>